{
  "name": "m-node-gtk",
  "version": "0.1.0",
  "description": "An Adwaita application built with node-gtk.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "GSK_RENDERER=cairo npm run dev:css-reload",
    "dev:css-reload": "cross-env NODE_ENV=development GSK_RENDERER=cairo node --import tsx --import node-gtk/register src/main.ts",
    "dev:app-reload": "cross-env NODE_ENV=development GSK_RENDERER=cairo node --import tsx --import node-gtk/register --watch src/main.ts",
    "start": "node --import tsx --import node-gtk/register src/main.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "generate-types": "node-gtk generate-types Gtk-4.0 Adw-1",
    "postinstall": "npm run generate-types"
  },
  "dependencies": {
    "i18next": "^26.3.6",
    "node-gtk": "^4.0.1",
    "pdfkit": "^0.19.1",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@types/i18next": "^12.1.0",
    "@types/node": "^22.0.0",
    "@types/pdfkit": "^0.17.6",
    "@types/uuid": "^10.0.0",
    "cross-env": "^7.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
